<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@color/aura_bg">

    <!-- 🔍 Ümar otsinguriba -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_search"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <EditText
            android:id="@+id/searchEditText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Otsi rakendust"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:textSize="16sp"
            android:textColor="@color/aura_text"
            android:textColorHint="@color/aura_muted"
            android:background="@android:color/transparent"
            android:padding="0dp" />

        <!-- Luup (dekoratiivne) -->
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="8dp"
            android:src="@android:drawable/ic_menu_search"
            android:contentDescription="@null"
            android:tint="@color/aura_muted" />
    </LinearLayout>

    <!-- 📱 Rakenduste grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/appsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="6dp" />

    <!-- 🚫 Tühja otsingu tekst -->
    <TextView
        android:id="@+id/emptyText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="Ei leidnud ühtegi rakendust"
        android:textSize="14sp"
        android:textColor="@color/aura_muted"
        android:visibility="gone" />

</LinearLayout>
