<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AURA Dark Theme -->
    <style name="Theme.Aura" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Peamised värvid -->
        <item name="colorPrimary">@color/aura_green</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <item name="colorSecondary">@color/aura_green_light</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- Taustad -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="colorSurface">#121212</item>
        <item name="colorOnSurface">@android:color/white</item>

        <!-- Tekst -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">#B3FFFFFF</item>

        <!-- Status bar / navigation -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Ripple / highlight -->
        <item name="colorControlHighlight">#33A5D6A7</item>
        <item name="colorControlActivated">@color/aura_green</item>

    </style>

</resources>
